/**
 * Server Bootstrap
 * Registers path aliases before starting the application
 */

// Register tsconfig paths for runtime resolution
import 'tsconfig-paths/register';

// Import and start the main application
import './index';

